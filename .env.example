# WebSocket multiplayer (deploy ws-server to Fly.io/Railway for production)
NEXT_PUBLIC_WS_URL=ws://localhost:3001

# Global leaderboard persistence (points to ws-server HTTP API)
LEADERBOARD_API_URL=http://localhost:3001

# WS server port (local only)
WS_PORT=3001